clone.webyverse.com {
	encode gzip zstd

	@static path /framerclone* /bg-atmosphere* /og-preview* /icon* /apple-touch-icon.png /favicon.ico /site.webmanifest
	header @static Cache-Control "public, max-age=31536000, immutable"

	reverse_proxy app:3000
}
